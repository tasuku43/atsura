schema_version: 1
effect: read
command:
  executable: go
  args_prefix: [run, ./tools/sourcefixture]
decision: allow
reason: Return only synthetic task identity and status.
invoke:
  append_args: ["--format=json"]
output:
  input: json
  select: [number, title, state]
  rename:
    - from: number
      to: id
  render: compact_json
